@layer base {
  .wrapper {
    min-height: 100vh;
    @apply my-0 max-w-screen-xl mx-auto flex justify-center items-start flex-col px-4;
  }

  main {
    @apply flex flex-col flex-auto w-full mx-auto;
  }
}
